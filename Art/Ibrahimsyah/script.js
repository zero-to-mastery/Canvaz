const canvas = document.getElementById('indonesia');
const ctx = canvas.getContext('2d');
const width = canvas.clientWidth;
const height = canvas.offsetHeight;

ctx.fillStyle = '#FF0000';
ctx.fillRect(0, 0, 500, 75);

ctx.stroke();
